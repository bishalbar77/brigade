node_modules/
.next/
out/
build/
dist/

# env — never commit real keys. SUPABASE_SERVICE_ROLE_KEY bypasses RLS entirely.
.env
.env.local
.env*.local

# Local working files, deliberately untracked.
# CLAUDE.md  — assistant instructions for this repo
# PROGRESS.md — running build state and credential status
# Both stay on disk; they're just not part of the public repo.
CLAUDE.md
PROGRESS.md

.DS_Store
*.pem
npm-debug.log*
yarn-error.log*
.vercel
*.tsbuildinfo
next-env.d.ts
coverage/

# Submission artifacts: the deck is committed, scratch exports are not
BLANK-TEMPLATE-DO-NOT-SUBMIT.pdf.bak
